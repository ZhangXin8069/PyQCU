# AGENTS.md — cpp.cuda.qcu.logs

C++ CUDA 后端（`cpp/cuda/qcu`）的本地运行日志目录：构建（`bash ./make.sh`）、后端测试（如 `examples/qcu/conftest.clover.multigrid.py`）、性能/收敛报告的生成输出。

## 注意

- 本地运行目录，**git 不跟踪**。
- 开发报告与测试输出的正式位置是仓库根 `logs/` 目录（文件模式见其 AGENTS.md）；此处只放后端本地产物。
