# testing

pyqcu.testing 目录的完整生成 skill：全组件集成测试（函数名 test_*，由 examples/*/conftest.py 引用），含参考 HDF5 数据验证与日志约定。

- 规范全文：`SKILL.md`（frontmatter description 为触发依据）
- 维护约定：更新内容时同步本文件与库级 `../AGENTS.md` 技能表（先读后写、最小改动、不代提交）
