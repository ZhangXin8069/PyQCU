# cuda

pyqcu.cuda 目录的完整生成 skill：C++ CUDA 后端（libqcu.so）的 Cython 桥接包；含 22 个 C 函数封装、params/argv/set_ptrs 参数协议与 _SET_INDEX_ 递增关键约束。

- 规范全文：`SKILL.md`（frontmatter description 为触发依据）
- 维护约定：更新内容时同步本文件与库级 `../AGENTS.md` 技能表（先读后写、最小改动、不代提交）
