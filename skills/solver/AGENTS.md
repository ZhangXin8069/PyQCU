# solver

pyqcu.solver 目录的完整生成 skill：BiCGStab(l) 与自适应多重网格 V-cycle 求解器，含 CUDA 后端集成与粗网格校正后状态重置（R3 fix）。

- 规范全文：`SKILL.md`（frontmatter description 为触发依据）
- 维护约定：更新内容时同步本文件与库级 `../AGENTS.md` 技能表（先读后写、最小改动、不代提交）
