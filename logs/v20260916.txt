下面未提到的部分的补充参考为{${HOME}/PyQCU/logs/v2026***.txt};
本次任务的工作目录为{${HOME}/PyQCU/docs};
本次任务的工作对象为{工作目录中的{PyQCU项目}}(主要是MultiGrid部分)(其中CUDA-C++部分为核心);
本次任务的对照目录为{${HOME}/PyQCU/refer/git-rep/quda};
本次任务的对照对象为{对照目录中的{QUDA项目}}(主要是MultiGrid部分);
本次任务的工作目标为[{在{工作对象}与{对照对象}的multigrid求解器中插入可开关(默认不启用)的性能测试功能,例如记录multigrid求解器各层中各个关键环节的耗时以及对应的各迭代前后的求解残差(可以使用'r'的norm来表示),必要时重新编译{工作对象}与{对照对象}},{完成上述前置目标后,对{对照对象}与{工作对象}进行全面的算法解析与对比测试(开启上述新增的性能测试功能),其中输出的报告格式参考{${HOME}/PyQCU/docs/report_multigrid_quda_pyqcu_20260909.tex}},{若{工作对象}相对于{对照对象}的加速效果不明显,则[{充分分析{对照对象}的MultiGrid算法的优势},{充分分析{工作对象}的MultiGrid算法的劣势},{结合上述的分析,全面优化{工作对象},直到显著超越{对照对象}}],最后重复上述其他目标}];
本次任务的默认数据读写目录为{${HOME}/PyQCU/data};
本次任务的任务量较大,但不允许省略任务内容;
本次任务的性质为[编写,测试(核心),分析,优化,......],非必要不改动无关文件;
本次任务的补充说明为[{先前的测试缺失详细的{对照对象}的性能记录,此次必须补充,建议充分利用{上述新增的性能测试功能}与{{对照对象}本身的日志记录功能}},{先前的{工作对象}的multigrid求解器最细层的总迭代次数过少,不太符合正常multigrid求解器的一般规律(以{对照对象}为例),存在错误记录的可能,可供参考的意见为{multigrid求解是在odd层次上做的，但是solver在even层次上做的，或者是反过来,需要修正,保持与{对照对象}一致的记录策略}},{先前的测试的[格子大小,数据精度,multigrid求解器的层数(先前仅有两层),multigrid求解器的其他预设参数]过于单一,本次测试需要在各个条件允许的情况下进行适当扩展,其中{multigrid求解器的层数}必须至少补充三层的情况},{本次测试需要在各个条件允许的情况下适当进行多卡测试,必要时优化{工作对象}的多卡并行性能}];
本次任务的权限较高,所有改动在git-commit后自动提交到git远程,包括最终的git-tag,并确保其中标注的{message}的信息详实且格式规范;
如果在本次任务中发现{工作对象}中存在其他的必要的[debug项,优化项,补充项],可以自行改动本库代码;
上述任务全部完成后~diff~init~tag(stab);