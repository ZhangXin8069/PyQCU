下面未提到的部分的补充参考为{${HOME}/PyQCU/logs/v2026***.txt};
本次任务的工作目录为{${HOME}/PyQCU};
本次任务的工作对象为{工作目录中的{PyQCU项目}(主要是MultiGrid部分)};
本次任务的对照目录为{${HOME}/PyQCU/refer/git-rep/quda};
本次任务的对照对象为{对照目录中的{quda项目}(主要是MultiGrid部分)};
本次任务的工作目标为[{充分理解{对照对象}的MultiGrid算法是如何实现的}(主要是各层的P矩阵和R矩阵和Clover和Gauge和各种算子和各种预处理是如何实现的,注意奇偶处理),{~report上述的理解},{在{工作对象}中复现上述的{{对照对象}的MultiGrid算法}}(保留已有的旧实现(旧实现的MG-0层(最细层)有奇偶处理,但粗层没有奇偶处理,粗层算子也不是完整的dslash算子,属于不完备的有缺陷的MultiGrid实现),作为新实现的补充对比)];
本次任务的数据读写目录为{${HOME}/PyQCU/data};
本次任务的权限较高,所有改动在备注comm后自动提交到git远程,包括git tag;
上述任务完成后~report此任务,并~tag(stab),并自动提交到git远程;
